# Minimal Dockerfile for runner service placeholder
FROM alpine:3.19

# Create the /runs directory for volume mounting
RUN mkdir -p /runs

# Default command that exits cleanly
CMD ["echo", "Runner service placeholder - exiting cleanly"]