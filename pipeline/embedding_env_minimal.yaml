name: embedding_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch
      - transformers
      - peft
      - tqdm
      - safetensors
      - accelerate